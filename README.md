ћожно настроить им€ основной ветки нового репозитори€, созданного с помощью init:

```$ git config --global init.defaultBranch main
```

¬ следующий раз git init создаст репозиторий с main в качестве основной ветки: